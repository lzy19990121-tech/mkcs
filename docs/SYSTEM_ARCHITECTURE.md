# MKCS 系统架构文档

> 明确 谁在什么时候说话
> 明确 哪些策略被启用 / 禁用 / 降权
> Market Analysis、MA、Breakout、ML、LLM 各司其职
> 所有决策 可解释、可回放、可复现

## 五层架构

```
┌─────────────────────────────────────────────────────────────┐
│  L5: LLM Advisor                                              │
│  - 盘后总结                                                   │
│  - 规则建议 (RuleProposal)                                    │
│  - 参数建议                                                   │
│  ❌ 不参与实时决策                                            │
└─────────────────────────────────────────────────────────────┘
                              ↕ (盘后异步)
┌─────────────────────────────────────────────────────────────┐
│  L4: Meta / Brain (MetaStrategyAgent)                         │
│  - 融合 AlphaOpinions                                         │
│  - 输出 MetaDecision (目标仓位)                               │
│  - 说明哪些策略启用/禁用                                       │
│  ✓ 唯一的"下决定的人"                                         │
└─────────────────────────────────────────────────────────────┘
                              ↕ (每个 bar)
┌─────────────────────────────────────────────────────────────┐
│  L3: Market Analysis (MarketState)                            │
│  - 描述市场状态                                               │
│  - 输出 MarketState                                          │
│  ❌ 不产生买卖建议                                            │
└─────────────────────────────────────────────────────────────┘
                              ↕ (每个 bar)
┌─────────────────────────────────────────────────────────────┐
│  L2: Alpha Strategies (MA / Breakout / ML)                    │
│  - 输出 AlphaOpinion (方向/强度/置信度)                        │
│  - ❌ 不决定仓位                                               │
│  - ❌ 不产生订单                                               │
└─────────────────────────────────────────────────────────────┘
                              ↕ (每个 bar)
┌─────────────────────────────────────────────────────────────┐
│  L1: Execution (下单 / 模拟)                                  │
│  - 接收 RiskDecision                                          │
│  - 输出 ExecutionResult                                       │
└─────────────────────────────────────────────────────────────┘

                              ↕ (风控决策)
┌─────────────────────────────────────────────────────────────┐
│  Risk Gate (RiskGateAgent)                                    │
│  - 接收 MetaDecision                                         │
│  - 接收 MarketState                                          │
│  - 输出 RiskDecision (调整后的仓位)                            │
└─────────────────────────────────────────────────────────────┘
```

## 层级职责边界

### L1: Execution 层

**文件**: `skills/execution/agent.py`

**职责**:
- 执行交易指令（paper / replay / live）
- 记录执行结果
- 记录滑点和成交价

**输入**: `RiskDecision`
**输出**: `ExecutionResult`

**边界**:
- ✓ 下单 / 模拟交易
- ✓ 记录成交结果
- ✗ 不做任何交易决策

### L2: Alpha 层

**文件**: `skills/strategy/alpha_base.py`

**职责**:
- 分析市场产生观点
- 输出方向、强度、置信度
- 给出理由

**输入**: `MarketState` (作为 context)
**输出**: `AlphaOpinion`

**边界**:
- ✓ 表达做多/做空观点
- ✓ 表达信心强度
- ✗ 不决定仓位
- ✗ 不产生订单

### L3: Market Analysis 层

**文件**: `skills/market_analysis/`

**职责**:
- 检测市场状态 (Regime, Volatility, Liquidity)
- 接入外部信号 (Sentiment, Macro, Events)
- 输出统一的市场状态

**输入**: K线数据, 外部数据
**输出**: `MarketState`

**边界**:
- ✓ 描述市场当前状态
- ✓ 描述市场历史特征
- ✗ 不产生任何买卖建议
- ✗ 不包含任何预测

### L4: Meta / Brain 层

**文件**: `skills/brain/meta_strategy.py`

**职责**:
- 融合多个 Alpha 观点
- 决定最终目标仓位
- 说明哪些策略被启用/禁用
- 处理策略冲突

**输入**: `MarketState`, `List[AlphaOpinion]`, 当前持仓
**输出**: `MetaDecision`

**边界**:
- ✓ 融合多策略观点
- ✓ 决定目标仓位
- ✓ 说明策略启用/禁用原因
- ✗ 不直接下单
- ✓ 同一输入 → 稳定输出 (可回放)

### L5: LLM Advisor 层

**文件**: `skills/llm/advisor.py`

**职责**:
- 盘后总结 runs
- 生成规则建议 (RuleProposal)
- 生成参数建议

**输入**: 历史运行数据
**输出**: `AdvisorReport`

**边界**:
- ✓ 盘后总结
- ✓ 规则建议
- ✓ 参数建议
- ✗ 不参与实时决策路径
- ✓ 系统在无 LLM 情况下仍可运行

### Risk Gate 层

**文件**: `skills/risk/risk_gate_agent.py`

**职责**:
- 基于 MetaDecision 调整仓位
- 基于 MarketState 应用风控规则
- 设置冷却期

**输入**: `MetaDecision`, `MarketState`, `AccountState`
**输出**: `RiskDecision`

**边界**:
- ✓ 优先缩放仓位，其次才 reject
- ✓ 风控动作有冷却，不抖动
- ✓ 极端行情下系统"自动保命"

## 数据流（每个 bar）

```
1. K线数据更新
   ↓
2. MarketAnalysis.analyze() → MarketState
   ↓
3. 每个 Alpha 策略:
   - 检查是否被 gating 禁用
   - 如果启用，生成 AlphaOpinion
   - 如果禁用，输出禁用的 AlphaOpinion (is_disabled=True)
   ↓
4. MetaStrategyAgent.decide():
   - 收集所有 AlphaOpinion
   - 应用融合规则
   - 输出 MetaDecision (目标仓位 + 启用/禁用列表)
   ↓
5. RiskGateAgent.check():
   - 检查 MarketState
   - 应用风控规则
   - 输出 RiskDecision (可能缩放的仓位)
   ↓
6. ExecutionAgent.execute():
   - 执行交易 (paper/live/replay)
   - 输出 ExecutionResult
```

## Alpha Gating 规则

| 策略 | 禁用 Regime | 降权 Regime | 禁用 Volatility | 降权 Volatility |
|------|-------------|-------------|-----------------|-----------------|
| MA | CRISIS | RANGE (0.3x) | - | EXTREME (0.2x), HIGH (0.5x) |
| Breakout | CRISIS, RANGE | - | - | EXTREME (0.3x) |
| ML | CRISIS | RANGE (0.5x) | - | EXTREME (0.1x) |

## Alpha 融合规则

### 共识型（多策略同向加权）
- 如果所有启用策略方向一致
- 仓位 = 加权平均

### 冲突型（强者优先 / 市场状态裁决）
- 如果策略方向冲突
- 优先选择置信度高的策略
- 或者根据 MarketState 选择合适的策略类型

### 无共识（空仓或轻仓）
- 如果策略冲突且置信度都不高
- 降低仓位或空仓

## Schema 版本控制

当前版本: `v1`

所有 schema 都包含 `schema_version` 字段，确保向后兼容。

## 禁用是一等状态

被禁用的策略不是 silent ignore，而是：
1. 输出 `is_disabled=True` 的 `AlphaOpinion`
2. 记录 `disabled_reason`
3. 在日志中明确记录
4. 在 MetaDecision 中出现在 `disabled_strategies`

这确保了"能明确回答：这个策略为什么今天没参与"

## 可回放性

- 所有决策都基于输入的 schema
- 所有 schema 可序列化
- 相同输入 → 相同输出
- 所有决策都有 `reason` 字段

## 可解释性

- 完整的决策链追溯
- HTML 格式决策报告
- 每个决策步骤都有说明
- 可视化策略启用/禁用状态

## 实现状态

| 组件 | 文件 | 状态 | 测试 |
|------|------|------|------|
| Schema 定义 | `core/schema.py` | ✅ 完成 | ✅ 通过 |
| L2 Alpha Strategies | `skills/strategy/alpha_base.py` | ✅ 完成 | ✅ 通过 |
| L4 Meta/Brain | `skills/brain/meta_strategy.py` | ✅ 完成 | ✅ 通过 |
| Risk Gate | `skills/risk/risk_gate_agent.py` | ✅ 完成 | ✅ 通过 |
| L1 Execution | `skills/execution/agent.py` | ✅ 完成 | ✅ 通过 |
| L5 LLM Advisor | `skills/llm/advisor.py` | ✅ 完成 | ✅ 通过 |
| UI/可解释性 | `skills/visualization/explainability.py` | ✅ 完成 | ✅ 通过 |
| 集成测试 | `tests/integration/test_full_pipeline.py` | ✅ 完成 | ✅ 通过 |
| 系统回归测试 | `tests/system/test_regression.py` | ✅ 完成 | ✅ 22/22 通过 |
| 系统验收测试 | `tests/system/test_acceptance.py` | ✅ 完成 | ✅ 37/37 通过 |

## 运行测试

```bash
# 单独测试各组件
python -m skills.strategy.alpha_base
python -m skills.brain.meta_strategy
python -m skills.risk.risk_gate_agent
python -m skills.execution.agent
python -m skills.llm.advisor
python -m skills.visualization.explainability

# 运行完整集成测试
PYTHONPATH=. python tests/integration/test_full_pipeline.py

# 运行系统回归测试
PYTHONPATH=. python tests/system/test_regression.py

# 运行系统验收测试
PYTHONPATH=. python tests/system/test_acceptance.py
```

## 测试覆盖场景

1. **正常趋势市场**: 策略正常启用，Meta 融合，风控通过，正常执行
2. **极端市场**: 策略被禁用/降权，风控缩放仓位
3. **危机模式**: 所有策略禁用，风控禁止交易
4. **边界条件**: 空观点列表、零资金、全禁用策略
5. **性能测试**: 100次迭代平均耗时 < 1ms
6. **完整数据流**: 5个时间步连续模拟
7. **LLM Advisor**: 盘后报告生成
8. **可解释性**: 决策链追溯和 HTML 报告

## 系统验收状态

✅ **Phase 0-8 全部完成**

- ✅ Phase 0: Schema 定义与系统角色
- ✅ Phase 1: Alpha 策略降级为观点输出
- ✅ Phase 3: MetaStrategyAgent 实现
- ✅ Phase 4: RiskGateAgent 实现
- ✅ Phase 5: ExecutionAgent 实现
- ✅ Phase 6: LLM Advisor 层实现
- ✅ Phase 7: UI/可解释性实现
- ✅ Phase 8: 系统级回归与验收

**验收结果**: 37/37 通过 (100%)

| 验收模块 | 状态 |
|---------|------|
| 一、系统职责与边界 | ✅ 4/4 |
| 二、Market Analysis 验收 | ✅ 5/5 |
| 三、Alpha 层验收 | ✅ 6/6 |
| 四、Brain / MetaStrategy 验收 | ✅ 5/5 |
| 五、Risk Gate 验收 | ✅ 3/3 |
| 六、Execution / Simulation 验收 | ✅ 2/2 |
| 七、可回放性与一致性 | ✅ 1/1 |
| 八、可解释性与 UI | ✅ 4/4 |
| 九、系统级行为验收 | ✅ 2/2 |
| 十、最终 5 问验收 | ✅ 5/5 |

**🎉 系统完成（10/10 全通过）- 可长期演进**
